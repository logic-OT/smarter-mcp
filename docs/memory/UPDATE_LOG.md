# Update Log — smarter-mcp

## Friday, 12-06-2026, 7:26 pm, [feat/production-hardening] CI/packaging/lint hardening
H17: `.github/workflows/ci.yml` — matrix py3.10–3.13, ruff + pytest, SHA-pinned actions (checkout v4.2.2, setup-uv v8.2.0). H6: rewrote `publish.yml` — tag-triggered (`v*`), CI gate job as prerequisite, OIDC trusted publishing (`id-token: write`), removed long-lived PYPI_TOKEN, `environment: pypi`, SHA-pinned. M20: ruff `select` expanded to E/F/I/W/B/UP/RUF/S; configured `E501`+`S104` ignores, `S101` per-file ignore for tests; 127 violations cleared (91 autofix + 36 manual — B904 chaining, B023 loop closure, B007 unused loop vars, E741 ambiguous names, S110/S108 targeted noqa). M20 mypy: `[tool.mypy]` added, `mypy>=1.10` in dev extra, non-blocking scope (future pass). M11: `src/smarter_mcp/py.typed` added, ships in wheel. M13: `__init__.py` now reads `importlib.metadata.version("smarter-mcp")` with fallback; pyproject.toml is single source of truth. A4: LICENSE + `license = "MIT"` verified present in sdist/wheel. H18: deleted stale root `smarter-mcp.yaml` (macOS pytest-tmpdir cruft). Lows: Alpha→Beta classifier, removed `pending.md`+`/docs/superpowers` from `.gitignore`. Spec moved `smarter-mcp-spec.md` → `docs/`. All 303 tests pass; ruff clean; `uv build` → 0.1.1.

## Friday, 12-06-2026, 7:07 pm, [wt/security] Critical security bypass fixes: C1–C3, I1–I3, M3–M5
Fixed C1 (DNS-rebinding/TOCTOU in SSRF guard — `_SSRFGuardedHTTP(S)Connection` now validates `sock.getpeername()` post-connect, defeating rebinding); C2 (IPv4-mapped IPv6 bypass — `::ffff:169.254.169.254` etc. now blocked via `_is_raw_ip_blocked` + `::ffff:0:0/96` in `_BLOCKED_NETWORKS`); C3 (dead `IPRateLimitMiddleware` — wired into `_asgi_middleware()` gated on `rate_limit_enabled`); I1 (`/schema` endpoint — inline auth gate added to `_schema_handler`); I2 (`hmac.compare_digest` key-length oracle — both operands SHA-256 hashed before comparison); I3 (fail-open on unparseable address — `except ValueError: continue` replaced with fail-closed); M3 (eager `anyio.Lock()` in `SlidingWindowMiddleware.__init__` — lazy init pattern); M4 (`0 < x > y` chained comparison fixed); M5 (unknown bucket comment added). 22 new negative tests (C1 rebinding, C2 IPv6 mapped, I3 fail-closed, I2 width-oracle, I1 schema auth, C3 e2e rate-limit wiring). 303 tests pass; ruff clean on changed files.

## Friday, 12-06-2026, 6:51 pm, [wt/docs] smarter-mcp-spec.md: 5 documentation defects corrected
smarter-mcp-spec.md: (1) `inherit_methods: true/false` → `include_inherited: true/false` (actual `ExposeConfig` field); (2) nested `server.rate_limit:` YAML block replaced with flat fields `rate_limit_enabled`, `rate_limit_per_minute`, `rate_limit_global_per_minute`; (3) nested `server.auth:` YAML block replaced with flat fields `auth_enabled`, `auth_header`, `auth_keys_env`; (4) removed stale `structlog>=23.0` and `jinja2>=3.0` core-dep rows (not in pyproject, not imported), total corrected to ~5.5MB; (5) stale "cleaned up on session end" wording updated to bounded LRU (max 256, OrderedDict, best-effort `close()`/`__exit__` on eviction) matching `runtime/instances.py` and line ~77 phrasing. Added historical-design-spec note at top of file.

## Friday, 12-06-2026, 6:37 pm, [wt/security] Security hardening: H1–H5, H7–H8, A1–A2, M18, b64decode
Fixed H1 (default host 127.0.0.1; loud non-loopback+no-auth startup warning), H2 (SSRF guard — URL fetch and local-file reads now OPT-IN with allow_url_fetch/allow_local_file; private-IP blocklist via hmac-safe getaddrinfo; anyio thread offload in async path), H3 (PIL.Image.MAX_IMAGE_PIXELS capped; base64 length cap; image_max_size now enforced, was dead config), b64decode (validate=True, CoercionError on garbage), H4 (tracebacks server-log-only; client gets sanitised JSON; tools raise ToolError so isError=True is set; debug_include_traceback flag), H5 (pickle→JSON cache with explicit IR encode/decode; CACHE_VERSION bumped to 2; cache anchored to source_root; no pickle import remains), H7 (hmac.compare_digest constant-time key compare; auth_enabled+no-keys → RuntimeError at startup via assert_auth_keys_present, called from build()), A2 (assert_auth_keys_present fires on every public entrypoint), H8 (/health unauthenticated returns only {status}; /schema missing namespace returns 404 not 200-with-error), A1 (SchemaEndpoint built from router-registered surface; expose=False tools absent), M18 (IPRateLimitMiddleware keyed on scope["client"] socket IP, not X-Forwarded-For). 71 new security tests; 281 total pass; ruff clean on changed files.

## Friday, 12-06-2026, 6:20 pm, [wt/docs] Documentation accuracy + LICENSE
README.md: removed hotlinked user-attachments banner (replaced with plain heading); deduped 3× "Schema validation" bullet → 1, restored missing "Type coercion" prefix; updated session-cleanup claim to bounded LRU wording; fixed LLM YAML (`cache: true` → `enabled: true` + `cache_path:`); fixed expose YAML (`private:` → `include_private:`, `unannotated:` → `unannotated_policy:`). docs/architecture.md: rebuilt scrambled CLI section; fixed Mermaid diagram (removed dead `base path` from routing, `cache`→`cache_path`/`overwrite`→`overwrite_existing` in LLM node). docs/README.md: fixed stale `pymcp` install path → `smarter-mcp`. smarter-mcp-spec.md: updated "Cleanup on session end" to bounded LRU wording. LICENSE: added standard MIT text (Copyright 2026 logic-OT).

## Friday, 12-06-2026, 6:03 pm, [feat/config-llm-deps] Code-review fixes: path portability, docs sweep, exception simplification, test hardening
Fixed 6 issues on `feat/config-llm-deps`: (1) `test_h19_deps.py` hardcoded absolute paths replaced with `_REPO_ROOT = Path(__file__).resolve().parents[2]`; (2) `image_format: png` removed from `docs/README.md` and `smarter-mcp-spec.md`; also fixed invalid field names in spec YAML (`class:`→`class_name:`, `constructor:`→`constructor_args:`, short expose keys→full names, `cache: true`→`cache_path:`, `transport: [...]`→scalar, `parameters:` tool blocks removed); (3) `smarter-mcp.yaml` commented routing stripped of removed `base_path`/`root_aggregate` keys; (4) `enrich_registry` dead `logger.warning + description = None` branches removed — only abort-class errors reach the outer handler; (5) `test_router_passes_auto_detect_false_to_wrapper` strengthened from smoke test to full FastMCP in-memory Client call; (6) `test_h19_llm_import_error_message_references_extra` fixed from vacuous (`except Exception: pass`) to direct `sys.modules["openai"] = None` pattern; (7) `_sanitize_description` hardened for unterminated fences with `_UNTERMINATED_FENCE_RE` fallback; new test added. 210 tests pass.

## Friday, 12-06-2026, 5:41 pm, [feat/discovery-api] Task 5 — dead config wiring complete
Wired remaining dead config fields: `server.log_level` sets root logger at `SmarterMCP.__init__`; `multimodal.auto_detect` gates `coerce_to_fastmcp_image` in both `_build_function_wrapper` and `_build_method_wrapper`, passed from `NamespaceRouter._register_tool`; `SourceConfig.include` for path sources now restricts `SurfaceExtractor._discover_files` via new `include_patterns` param, wired through `discover()` and `build()`. Fixed `sys.modules` pollution in test fixture that caused order-dependent failure in CLI init test. All 209 tests pass.

## Friday, 12-06-2026, 4:57 pm, [feat/discovery-api] PR4 — discovery + API contract fixes
Resolved C3 (ctor arg order: `name` first, `source_root` kw-only, nonexistent path raises, `find_manifest` bounded at VCS boundary), C4 (`discover_module` works for packages + classes, invocable end-to-end), C5 (extraction errors surfaced in discover/validate/health, `validate` exits non-zero), H12 (decorator tools served unprefixed; full dotted-path namespaces; collision warnings), H13 (`@property` resources bound via InstanceManager), H14 (manifest source paths resolved relative to manifest dir), M6 (import-failure ERROR + aggregate summary, exposed in /health), M8 (`extraction_result` populated). Two-stage review caught a tuple-unpack crash in `init`, a `rstrip(".py")` name-mangling bug, in-place ExtractionResult cache mutation, and an H13 test that didn't test the fix — all fixed. 153 tests pass; harness C3/C4a/C4b/C5 NOT REPRODUCED.

## Friday, 12-06-2026, 2:30 pm, [feat/schema-coercion] PR3 — schema + coercion correctness (merged #7)
Single shared `_typeparse.py` normalizer now drives both schema generation and coercion. H11 (Optional/Union/List/Dict/Literal map to correct JSON Schema with anyOf/items/enum), H9 (no RecursionError on `list[int | None]`), strict bool/int coercion (reject unrecognized bool strings, non-integral/non-finite floats), M19 (1 MiB json.loads guard), M3 (non-literal defaults use a sentinel), element-wise coercion so `list[int]` input matches the published schema. 119 tests pass; harness H9/H11 NOT REPRODUCED.

## Friday, 12-06-2026, 1:00 pm, [feat/runtime-core] PR2 — runtime core repair (merged #6)
C1 (str/bytes/Path returns no longer coerced into Image), C2 (session lifecycle keeps one instance per session via `get_context()`; bounded LRU eviction with best-effort close outside the lock), H10 (static/classmethods route correctly), H20 (thread-safe singleton/session creation), M5 (Context injected by actual param name incl. Optional/union), M4 (`build()` idempotent). 65 tests pass; harness C1/C2 NOT REPRODUCED.

## Friday, 12-06-2026, 11:42 am, [main] Principal addendum appended to production assessment

Codex RTK follow-up added a non-duplicative section to `docs/reviews/2026-06-12-production-assessment.md`: validates Fable 5's verdict, sharpens the false-confidence risk in `smarter-mcp test`, and adds schema leak, public auth-entrypoint split, FastMCP compatibility, and missing license-file gaps.

## Friday, 12-06-2026, 11:22 am, [main] Full production assessment written to docs/reviews/

Five-dimension parallel review (security, correctness, robustness, DX/docs/CI, performance) of the whole codebase. 5 Critical findings, all reproduced by execution: str-returning tools broken (image coercion), session lifecycle inert (Context never injected), `SmarterMCP("name")` positional-arg trap, `discover_module` broken for packages/classes, extraction errors silently discarded. See `docs/reviews/2026-06-12-production-assessment.md` for the full report and ordered remediation roadmap. Ground truth: 53 tests pass (extractor-only coverage), 112 ruff errors, no CI, `/tests` gitignored.
